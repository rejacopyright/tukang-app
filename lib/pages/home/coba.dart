import 'package:flutter/material.dart';

class HomeCoba extends StatelessWidget {
  const HomeCoba({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('HOME'));
  }
}
